<Window x:Class="unity_cli_ui.ModuleInstallerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="{DynamicResource Loc.S8A1AD0702ECB807E}"
        Width="640"
        Height="664"
        MinWidth="560"
        MinHeight="524"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        WindowStyle="None"
        Background="Transparent"
        FontFamily="Segoe UI, Microsoft YaHei UI"
        TextOptions.TextFormattingMode="Display"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        StateChanged="Window_StateChanged">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0"
                            ResizeBorderThickness="7"
                            GlassFrameThickness="0"
                            CornerRadius="0"
                            UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>
    <Window.Resources>
        <Style x:Key="DialogButton" TargetType="Button">
            <Setter Property="Height" Value="34" />
            <Setter Property="MinWidth" Value="82" />
            <Setter Property="Padding" Value="13,0" />
            <Setter Property="Background" Value="White" />
            <Setter Property="Foreground" Value="#344054" />
            <Setter Property="BorderBrush" Value="#D7DCE5" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Surface"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Surface" Property="Background" Value="#F0F2F5" /></Trigger>
                            <Trigger Property="IsPressed" Value="True"><Setter TargetName="Surface" Property="Background" Value="#E5E8ED" /></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="DialogPrimaryButton" TargetType="Button" BasedOn="{StaticResource DialogButton}">
            <Setter Property="Background" Value="#0078D4" />
            <Setter Property="BorderBrush" Value="#0078D4" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Surface"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Surface" Property="Background" Value="#106EBE" /></Trigger>
                            <Trigger Property="IsPressed" Value="True"><Setter TargetName="Surface" Property="Background" Value="#005A9E" /></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="DialogCaptionButton" TargetType="Button">
            <Setter Property="Width" Value="46" />
            <Setter Property="Height" Value="44" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#394256" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
            <Setter Property="FontSize" Value="10" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="shell:WindowChrome.IsHitTestVisibleInChrome" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Surface" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Surface" Property="Background" Value="#18000000" /></Trigger>
                            <Trigger Property="IsPressed" Value="True"><Setter TargetName="Surface" Property="Background" Value="#28000000" /></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="DialogCloseButton" TargetType="Button" BasedOn="{StaticResource DialogCaptionButton}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Surface" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Surface" Property="Background" Value="#E81123" /><Setter Property="Foreground" Value="White" /></Trigger>
                            <Trigger Property="IsPressed" Value="True"><Setter TargetName="Surface" Property="Background" Value="#C50F1F" /><Setter Property="Foreground" Value="White" /></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ListBox">
            <Setter Property="ScrollViewer.CanContentScroll" Value="False" />
            <Setter Property="ScrollViewer.IsDeferredScrollingEnabled" Value="False" />
            <Setter Property="ScrollViewer.PanningMode" Value="VerticalFirst" />
        </Style>
    </Window.Resources>

    <Border BorderBrush="#D9DEE8" BorderThickness="1" Background="Transparent">
      <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="44" />
            <RowDefinition Height="76" />
            <RowDefinition Height="*" />
            <RowDefinition Height="112" />
        </Grid.RowDefinitions>

        <Border x:Name="TitleBarSurface" Grid.Row="0" Background="#18FFFFFF" BorderBrush="#E1E5EB" BorderThickness="0,0,0,1" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid>
                <TextBlock Text="{DynamicResource Loc.S8A1AD0702ECB807E}" Foreground="#303A4C" FontSize="12" FontWeight="SemiBold" Margin="16,0,0,0" VerticalAlignment="Center" />
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="&#xE921;" ToolTip="{DynamicResource Loc.SAC29E57A46F41C30}" Style="{StaticResource DialogCaptionButton}" Click="Minimize_Click" />
                    <Button x:Name="MaximizeButton" Content="&#xE922;" ToolTip="{DynamicResource Loc.S82C4AA13B7CA109E}" Style="{StaticResource DialogCaptionButton}" Click="Maximize_Click" />
                    <Button Content="&#xE8BB;" ToolTip="{DynamicResource Loc.S3FD47EDCE45B3603}" Style="{StaticResource DialogCloseButton}" Click="Close_Click" />
                </StackPanel>
            </Grid>
        </Border>

        <Border x:Name="HeaderSurface" Grid.Row="1" Background="#18FFFFFF" BorderBrush="#E1E5EB" BorderThickness="0,0,0,1" Padding="22,15">
            <StackPanel>
                <TextBlock Text="{DynamicResource Loc.S5F3DACB9CCB02DE1}" Foreground="#202B3E" FontSize="19" FontWeight="SemiBold" />
                <TextBlock Text="{Binding HeaderText}" Foreground="#7D8798" FontSize="11" Margin="0,5,0,0" />
            </StackPanel>
        </Border>

        <Grid Grid.Row="2" Background="#F7F8FA" Margin="0">
          <Grid Margin="22,16,22,10">
            <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="10" /><RowDefinition Height="*" /></Grid.RowDefinitions>
            <Grid>
                <TextBlock Text="{DynamicResource Loc.S83A4380B232411B9}" Foreground="#2D394D" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center" />
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="{DynamicResource Loc.S3A5040B68ABF75F9}" Style="{StaticResource DialogButton}" Height="30" MinWidth="60" Padding="10,0" Click="SelectAll_Click" />
                    <Button Content="{DynamicResource Loc.SBCE2377283C2455A}" Style="{StaticResource DialogButton}" Height="30" MinWidth="60" Padding="10,0" Margin="7,0,0,0" Click="ClearSelection_Click" />
                </StackPanel>
            </Grid>

            <Border Grid.Row="2" Background="White" BorderBrush="#DEE3EA" BorderThickness="1">
                <ListBox ItemsSource="{Binding Modules}" Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="IsTabStop" Value="False" />
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#E8EBF0" BorderThickness="0,0,0,1" Padding="14,0" Height="58">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="28" />
                                        <ColumnDefinition Width="34" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="90" />
                                    </Grid.ColumnDefinitions>
                                    <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    <Border Grid.Column="1" Width="28" Height="28" Background="#F0F2F5" BorderBrush="#E1E5EB" BorderThickness="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Glyph}" FontFamily="Segoe MDL2 Assets" Foreground="#5F6B7C" FontSize="15" TextAlignment="Center" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </Border>
                                    <StackPanel Grid.Column="2" VerticalAlignment="Center" Margin="8,0,10,0">
                                        <TextBlock Text="{Binding Name}" Foreground="#3F4B60" TextTrimming="CharacterEllipsis" />
                                        <TextBlock Text="{Binding Id}" Foreground="#9AA3B2" FontSize="10" TextTrimming="CharacterEllipsis" Margin="0,3,0,0" />
                                    </StackPanel>
                                    <TextBlock Grid.Column="3" Text="{Binding Size}" Foreground="#8E98A9" FontSize="10" HorizontalAlignment="Right" VerticalAlignment="Center" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>
          </Grid>
        </Grid>

        <Border Grid.Row="3" Background="White" BorderBrush="#E1E5EB" BorderThickness="0,1,0,0" Padding="22,13">
            <Grid>
                <StackPanel VerticalAlignment="Center">
                    <CheckBox x:Name="DryRunCheck" Content="{DynamicResource Loc.S0589F82B58DC8449}" Foreground="#566176" />
                    <CheckBox x:Name="AcceptEulaCheck" Content="{DynamicResource Loc.S9910414CF92A2DFB}" Foreground="#566176" Margin="0,8,0,0" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
                    <Button Content="{DynamicResource Loc.S2CD0F3BE8738A86C}" Style="{StaticResource DialogButton}" IsCancel="True" />
                    <Button Content="{DynamicResource Loc.SF01FE62EF9D636C7}" Style="{StaticResource DialogPrimaryButton}" MinWidth="118" Margin="8,0,0,0" Click="Install_Click" />
                </StackPanel>
            </Grid>
        </Border>
      </Grid>
    </Border>
</Window>
