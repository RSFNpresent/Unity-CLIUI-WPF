# 最近工作日志

## 2026-07-27

- 将产品版本更新为 `v1.0.3`，发布说明聚焦网络不可用时仍可读取已安装模块。
- 加入 Win8/Win10 风格页面、窗口生命周期和亚克力遮罩过渡；当前集中策略强制启用动画。
- 按验收反馈增加动画时长与移动距离，左侧导航按钮改为 Win8 磁贴式缩放、水平移入和错峰进入。
- 修复首次显示和启动后立即切页动画被布局吞掉的问题，统一为指数减速飞入。
- 为两套 Win10 下拉框模板加入缩放淡入，为列表、滚动页和下拉列表加入平滑滚轮，并将亚克力遮罩限制到标题栏与导航栏。
- 将亚克力遮罩移到文本后方；增强并延后左侧按钮缩放飞入，避免被整窗淡入掩盖。
- 固定亚克力过渡的三层绘制顺序，遮罩位于标题栏/导航栏表面之后，避免渐变覆盖左侧文字。
- 动画验收包 `animation-test-3` 已通过 Release 构建（0 警告、0 错误）和 14/14 回归测试；self-contained 目录仅含单个 `Unity-CLIUI.exe`，启动后进入可交互状态。
- 按 Win8 全屏开始菜单方向重做左侧按钮入口姿态：从右侧轻微偏移、缩小且半透明状态放大归位；淡蓝选中背景和强调线加入双向滑动过渡。
- 亚克力激活/失活过渡改为最新请求优先，新焦点事件会打断当前动画并取消旧队列，避免停止快速切换后仍反复播放。
- `animation-test-4` 通过 Release 构建（0 警告、0 错误）和 14/14 回归测试；self-contained 输出仍为单 EXE，启动后进入可交互状态。
- 最小化、最大化和还原从指数/反向缓动统一调整为三次 `EaseOut`，使主要位移先快速完成、接近终点后再减速收束。
- `animation-test-5` 通过 Release 构建（0 警告、0 错误）和 14/14 回归测试；单 EXE 验收窗口已正常启动。
- 修复窗口状态动画被亚克力遮罩排队阻塞：启动时登记原生材质状态，最小化、最大化、还原和关闭会打断遮罩并优先执行；通用动画时钟拆分为独立小模块。
- 最大化/还原删除前置内缩阶段，改为直接进入目标窗口尺寸后播放一次归位动画。
- `animation-test-6` 通过 Release 构建（0 警告、0 错误）和 14/14 回归测试；单 EXE 验收窗口已正常启动。
- 窗口状态动画不再剔除亚克力遮罩：最大化、还原、最小化、关闭和首次进入由同一状态操作同步驱动内容变换与遮罩淡变。
- `animation-test-7` 通过 Release 构建（0 警告、0 错误）和 14/14 回归测试；同步遮罩动画的单 EXE 验收窗口已正常启动。
- `animation-test-8` 增强亚克力背景动画可见性：遮罩加入冷灰蓝轻渐变、22px 横向扫动，提高最大化/还原初始强度并延长至 360ms；Release 构建与 14/14 回归测试通过。
- 最小化和关闭改为 280ms 纯透明度退出，内容不再缩放或移动，亚克力渐变由当前强度淡至完全透明。
- `animation-test-9` 通过 Release 构建（0 警告、0 错误）、14/14 回归测试、单 EXE 布局和正常关闭退出验证。
- 最大化/还原移除全部动画并直接切换最终状态；最小化改为不缩放、向下 72px 三次加速离场；关闭改为立即关闭。
- 关闭请求在同步缓存保存前先隐藏窗口，使窗口视觉上立即消失，同时保留完整的 `Closed` 持久化流程。
- `animation-test-11` 通过 Release 构建（0 警告、0 错误）、14/14 回归测试和单 EXE 检查；关闭探针窗口约 215ms 消失、进程约 258ms 退出。
- 从最小化恢复改为不缩放、从下方 72px 向上减速平移归位，与向下加速最小化形成成对轨迹。
- 移除最小化及恢复时亚克力遮罩的 22px 水平扫动，垂直状态切换不再产生左右偏移。
- `animation-test-12` 通过 Release 构建（0 警告、0 错误）、14/14 回归测试、单 EXE 检查和直接关闭退出验证。
- 修复最小化时静止不透明底板：72px 下移/上移轨迹改为移动原生 HWND，WPF 内容仅做透明度，DWM 亚克力背景随整个窗口同步移动。
- `animation-test-13` 通过 Release 构建（0 警告、0 错误）、14/14 回归测试与单 EXE/禁止文件检查；PID `11652` 已启动并保持响应，等待人工确认最小化和恢复轨迹。
- 重做关闭动画：捕获包含 DWM 亚克力底板的完整合成窗口，以不激活的透明覆盖窗在 280ms 内整体淡出；原窗口不缩放、不移动，淡出后只执行一次真实关闭，取消时恢复原窗口。
- 放弃运行时 `WS_EX_LAYERED` 和 `AnimateWindow` 方案：前者被当前 WPF `HwndSource` 拒绝并返回错误 87，后者会服从系统动画开关，无法满足应用内强制动画。
- `animation-test-16` 通过 Release 构建（0 警告、0 错误）、14/14 回归测试和单 EXE/禁止文件检查；两次关闭探针为 483ms/460ms 并正常退出，PID `21752` 已启动并保持响应，等待人工确认底板同步淡出。
- 启动时左侧导航磁贴的淡入起点由 32% 调整为 0%，缩放、位移、错峰间隔、缓动和时长保持不变。
- `animation-test-17` 通过 Release 构建（0 警告、0 错误）、14/14 回归测试和单 EXE/禁止文件检查；PID `3824` 已启动并保持响应，等待人工确认完全透明起始帧。
- 设置页新增独立“关于”控件，从程序集元数据显示版本和当前仓库 `https://github.com/RSFNpresent/Unity-CLIUI-WPF`，仓库地址可用系统浏览器打开。
- 动画策略由强制启用改为跟随 Windows 客户端区域动画与菜单动画设置；动画关闭时页面、导航和磁贴立即恢复最终姿态，不残留透明、缩小或偏移状态。
- `v1.0.3` 发布候选通过 Release 构建（0 警告、0 错误）、14/14 回归、双包单 EXE/禁止文件/版本元数据检查；系统动画关闭环境下两种包均正常启动和立即关闭，“关于”内容可见。
- `v1.0.3` ZIP SHA-256：framework-dependent `A091D145B86BEA42BEFBEE7220394F3218DEC4348A051BB6F9B9075653A178C7`；self-contained `0E6AE4564FB1BE2DDD48760A547685EBCC940DB90A32402B64ACBD364257E1FD`。
- 已发布 `v1.0.3`：发布提交 `5077b3e`，annotated tag 与两个 ZIP 已推送；GitHub Release 为非草稿、非预发布，说明为 `animation and about`，远端资产大小和 SHA-256 回查一致。
- 动画策略测试加入回归入口，测试总数更新为 14。
- 建立 `.agents` 项目规范目录及分功能 knowledge 文档。
- Direct 模式的版本详情改为只显示本机已安装模块，不再优先展示历史全量模块缓存。
- Unity 远程模块目录读取失败时，回退到文件系统扫描与已登记模块的并集。
- 显式模块管理流程仍可联网读取官方完整目录，保留无 CLI 模块安装能力。
- 新增管理模式和本地模块回退测试；`v1.0.3` Release 构建 0 警告、0 错误，回归测试 14/14。
- framework-dependent 与 self-contained 发布物均通过单 EXE 布局、版本元数据、启动响应和禁止文件检查。

## 2026-07-26

- 发布 `v1.0.2`，提交为 `a4d54a2`，`main` 和标签已推送至 GitHub。
- 生成 framework-dependent 与 self-contained 两个 Windows x64 ZIP；每个 ZIP 仅包含 `Unity-CLIUI.exe`。
- Release 构建 0 警告、0 错误；直接后端测试 13/13；两个 EXE 均完成启动验证。
- 新增最小 Unity 工程创建；包版本读取自所选编辑器官方元数据，创建后加入列表并用对应编辑器打开。

## 2026-07-25

- 完成无 CLI 编辑器管理、首次使用窗口和 Windows 10 风格界面调整。
- 无 CLI 后端支持官方 Release API、最多三个包并行断点下载、完整性校验、安全 staging 解压及模块依赖。
- 调整工程列表表头分隔线、版本和最后打开时间的居中关系，并统一相关下拉框样式。
- 发布 `v1.0.0` 和 `v1.0.1`；后续版本不得覆盖已有标签。

## 记录格式

后续条目按日期简要记录行为变化、涉及模块、测试结果、提交/发布状态和遗留风险。未完成事项不要写成已完成。
