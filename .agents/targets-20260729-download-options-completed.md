1. -[x] Add Direct download path options - settings UI and ManagerSettings store editor root, package cache path, and keep-cache flag.
2. -[x] Apply Direct download path options - Direct install requests use the selected cache path and cleanup policy.
3. -[x] Verify build and tests - Release build passed and Direct regression tests passed.
