1. -[x] Add editor install confirmation dialog - show version, modules, and install directory with modify, confirm, and cancel buttons.
2. -[x] Route modify-directory behavior - Direct mode updates the selected root, CLI mode shows the manual install choice.
3. -[x] Check download concurrency - current Direct backend uses package-level parallel download with maximum concurrency 3.
4. -[x] Verify build and tests - Release build passed and Direct regression tests passed.
