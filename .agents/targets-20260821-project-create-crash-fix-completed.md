# 创建工程闪退修复目标

## 短期目标

1. - [x] 修复无 CLI 手动扫描的版本来源 - 使用 `Unity.exe` 元数据，不使用目录名。
2. - [x] 修复创建窗口的预期数据错误边界 - 显示错误，不终止 WPF 进程。
3. - [x] 增加回归检查并完成 Release 构建 - 0 警告、0 错误，18/18 测试通过。

## 长期目标

1. - [x] 无 CLI 和 CLI 模式使用同一 Unity 版本规则。
